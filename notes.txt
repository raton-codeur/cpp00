



-
interdit

C++11
Boost
*printf()
*alloc()
free()
using namespace <ns_name>
friend
STL
Containers (vector/list/map/...)
Algorithmes (include <algorithm>)
fonction dans un fichier d'en-tête sauf fonction template
-


-
classe sous forme canonique de Coplien

C()
constructeur par défaut

C(const C&)
constructeur de recopie
déclaration par recopie d'un autre objet
utilisé par f(C c) pour faire un objet local à f
utilisé par le retour d'une fonction, sur la pile

~C
destructeur

C& operator=(const C&)
opérateur d'affectation
dupliquer les valeurs d'un objet dans un autre
-


-
std::toupper(c) → c en majuscule
-



afficher une chaine sur au moins 10 caractères
std::cout << std::setw(10) << s;
il faut inclure #include <iomanip>